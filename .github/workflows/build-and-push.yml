name: Build and Push Docker Image

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE_NAME: omni-app

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 純測試 build（不需要 secrets）
      - name: Build Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: omni-api/Dockerfile
          push: false
          tags: ${{ env.IMAGE_NAME }}:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ---- ACR push：設好 secrets 後取消下面的註解，刪掉上面的 Build Docker Image ----
      # - name: Login to Alibaba Cloud ACR
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ${{ secrets.ACR_REGISTRY }}
      #     username: ${{ secrets.ACR_USERNAME }}
      #     password: ${{ secrets.ACR_PASSWORD }}
      #
      # - name: Build and Push
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: .
      #     file: omni-api/Dockerfile
      #     push: true
      #     tags: |
      #       ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/${{ env.IMAGE_NAME }}:latest
      #       ${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max
